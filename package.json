{
  "name": "cucumbe",
  "version": "1.0.0",
  "description": "The home of cucumbe.",
  "author": "André Matthies",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "nuxt": "^2.4.3",
    "vuetify": "^1.5.2"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.7.2",
    "nodemon": "^1.18.10",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2"
  }
}
